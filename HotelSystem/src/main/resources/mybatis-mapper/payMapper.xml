<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hotel.management.v1.pay.dao.PayDao">
	<insert id="bookadd">
		insert into kakaopay(tid,partner_order_id,bookno) values(#{tid},#{uuid},(select bookno from book where username = #{username} and bookstatus = '체크인대기'))
	</insert>
	
	<insert id="tossbookAdd">
		insert into tosspay(paymentKey,orderId,bookno) values(#{paymentKey},#{orderId},(select bookno from book where username = #{username} and bookstatus = '체크인대기'))
	</insert>
</mapper>
