<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hotel.management.v1.pay.dao.PayDao">
	<update id="kakaobookadd">
		update payment set tid = #{tid},
		 item_name =#{itemname},
		partner_order_id=#{orderid},total_amount = #{amount}
		, paytype = #{type} where bookno =(select max(b.bookno) from book b,payment p where b.bookno = p.bookno) 
	</update>
	<update id="paymenttoss">
		update payment set tid = #{tid},
		 item_name =#{itemname},
		partner_order_id=#{orderid},total_amount = #{amount}
		, paytype = #{type} where bookno =(select max(b.bookno) from book b,payment p where b.bookno = p.bookno) 
	</update>
	<update id = "mangeradd">
		update payment set paytype = #{paytype},item_name =#{gradename} where bookno = (select max(b.bookno) from book b,payment p where b.bookno = p.bookno)
	</update>
</mapper>
