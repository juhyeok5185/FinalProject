<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hotel.management.v1.manager.mall.Dao.ManagerMallListDao">
	
	<!-- 관리자 : 김동욱 -->
	

	<select id="mallsearch" resultType="hotel.management.v1.manager.mall.Dto.ManagerMallListDto$MallListSearch">
		select o.username, u.tel, u.name ,od.orderNo, od.orderDate, od.contactDate, od.orderPrice, od.orderEa 
		from orderdetail od
		left join orders o on o.orderNo = od.orderNo
		left join users u on u.username = o.username
	</select>
	
</mapper>